# ---------------------------------------------------------------------------
# App profile: ApiDemos (Android sample app).
# Selected with -Dapp=apidemos. Values verified against the committed APK manifest.
# ---------------------------------------------------------------------------

app.display.name=ApiDemos

android.app.path=apps/ApiDemos-debug.apk
android.app.package=io.appium.android.apis
android.app.activity=.ApiDemos
android.app.wait.activity=*

# ApiDemos is Android-only; there is no iOS build of it.
ios.app.path=
ios.app.bundle.id=
